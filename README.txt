"Provector" is a data structure class that allows you to save different kins of data
Just like a normal vector is uses a dynamic array, but the difference is ...

"Provector" contains three different modes (push back fast mode, push front fast mode, and normal).
In each mode it allows you to do one operation faster and better